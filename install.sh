# install brew #
################
ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

# install usual installs I do #
###############################
brew install bash-completion curl gdbm libyaml openssl readline screenfetch unar brew-cask emacs grc node python ruby sqlite wget

# install gem #
###############
gem install jekyll pro